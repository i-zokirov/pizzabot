export enum DlQueryType {
    Text = "text",
    Event = "evennt",
}

export enum TelegramResponseType {
    Text = "text",
    Card = "card",
    Image = "image",
    QuickReply = "quickReply",
    Payload = "payload",
}
